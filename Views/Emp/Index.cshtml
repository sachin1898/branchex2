@model IEnumerable<WebAppEx2.Models.Emp>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>


<div class="container">@Html.Partial("Ourpview")</div>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EDesig)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EDOJ)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EDesig)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EDOJ)
            </td>
          
        </tr>
    }

</table>
